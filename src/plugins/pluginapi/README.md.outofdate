### 本插件开发说明

该插件负责为外界提供获取/修改bot内部数据的API接口，该插件挂在于Nonebot自带的FastAPI应用中，无需单独启动

新增API流程：
1.在插件名.py中定义返回数据格式（位于api/schemas），可参考其他格式编写
2.在主程序添加API，需要在.get()中写上：
    response_model=你刚刚定义的数据格式
    summary="接口名称/用处"
    description="描述这个接口是干什么的"
3.编写你的程序，记得按你定义的数据接口返回参数

API文档：http://127.0.0.1:8080/docs